/** @type {import('next').NextConfig} */
const nextConfig = {
  // Usage/reference data (lemma-forms etc.) is large JSON imported at build
  // time on the server; keep the default. Nothing special needed yet.
};

export default nextConfig;
